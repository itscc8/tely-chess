# Project Structure
- main folder contains a client, a server folder, readme and a package.json with scripts to run dev or run production - this will cd into client run the build command and it will output a public folder inside server to serve via bun's performant http server.
